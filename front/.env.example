# ============================================================================
# Frontend Environment Configuration
# ============================================================================
# This file shows environment variables that MUST be set by the user.
# Other configuration is handled by compose.yaml
#
# Usage:
#   1. Copy this file: cp .env.example .env
#   2. Generate and set the required values below
#   3. Run: docker compose up
#
# ============================================================================

# CSRF token signing key (REQUIRED - generate a random key for production)
# Generate with: openssl rand -hex 32
CSRF_KEY=change-me-in-production-please-use-random-key

# ============================================================================
# Note: The following are configured in compose.yaml and should NOT be set here:
# - PORT (default: 3000)
# - COLLECTOR_URL (default: http://collector:8080)
# - ENV (default: production)
# - LOG_LEVEL (default: info)
# ============================================================================
