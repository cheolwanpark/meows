# Copy to .env and set these required values

MEOWS_ENCRYPTION_KEY=change-me-to-32-byte-random-key # 32-byte encryption key (generate: openssl rand -base64 32 | cut -c1-32)
CSRF_KEY=change-me-in-production-please-use-random-key # CSRF protection key (generate: openssl rand -hex 32)
