# Meows Configuration
# Copy this file to .env and customize with your values
# Setup: cp .env.example .env && chmod 600 .env

# Collector - Database
COLLECTOR_DB_PATH=/data/meows.db

# Collector - Server
COLLECTOR_PORT=8080
COLLECTOR_LOG_LEVEL=info
COLLECTOR_ENABLE_SWAGGER=true
COLLECTOR_MAX_COMMENT_DEPTH=5

# Collector - Schedule
# Cron expression for when to run all sources
# Examples:
#   "0 */6 * * *"   - Every 6 hours
#   "0 0 * * *"     - Daily at midnight
#   "*/30 * * * *"  - Every 30 minutes
COLLECTOR_CRON_EXPR=0 */6 * * *

# Collector - Rate Limits (milliseconds)
# Delay between API requests to avoid rate limiting
COLLECTOR_REDDIT_DELAY_MS=2000
COLLECTOR_SEMANTIC_SCHOLAR_DELAY_MS=1000

# Collector - Reddit Credentials
# Get these from: https://www.reddit.com/prefs/apps
# Create an app with type "script"
COLLECTOR_REDDIT_CLIENT_ID=your-reddit-client-id
COLLECTOR_REDDIT_CLIENT_SECRET=your-reddit-client-secret
COLLECTOR_REDDIT_USERNAME=your-username
COLLECTOR_REDDIT_PASSWORD=your-password

# Collector - Semantic Scholar API
# Get your API key from: https://www.semanticscholar.org/product/api
# Free tier: 1 req/sec, 5000 req/5min
COLLECTOR_SEMANTIC_SCHOLAR_API_KEY=your-semantic-scholar-api-key

# Frontend - Server
FRONTEND_PORT=3000

# Frontend - Collector Connection
# For Docker Compose, use service name: http://collector:8080
# For local development without Docker: http://localhost:8080
FRONTEND_COLLECTOR_URL=http://collector:8080
